use mluau::prelude::*;
use crate::prelude::*;

/// cli.prompt.text(message: string): string?
pub fn prompt_text(luau: &Lua, mut multivalue: LuaMultiValue) -> LuaValueResult {
    let function_name = "cli.prompt.text(message: string)";
    todo!()
}